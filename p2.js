const fmtMoney=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0});const fmtMoney2=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'});const fmtNum=new Intl.NumberFormat('en-US',{maximumFractionDigits:1});
const navSections=[['OVERVIEW',[['overview','◫','Operations Center']]],['ORDERS',[['purchase','↙','Purchase Orders'],['sales','↗','Sales Orders']]],['WAREHOUSE',[['receiving','⇩','Receiving'],['shipping','⇧','Shipping'],['inventory','▦','Inventory'],['packing','▣','Packing Area']]],['PLANNING',[['replenishment','↻','Replenishment']]],['MASTER DATA',[['products','□','Products'],['parties','♧','Customers & Vendors']]],['INTELLIGENCE',[['analytics','⌁','Analytics']]],['TOOLS',[['amazon','A','Amazon'],['scanner','⌗','Scanner']]],['SYSTEM',[['admin','⚙','Admin']]]];
const state={page:'overview',inventoryTab:'racks',filters:{},compact:false,selectedRack:'R12'};
const data={kpis:{inventory:842760,openSales:186430,openPurchases:94120,warehouse:76,margin:17.8},purchaseOrders:[{id:'PO-22714',date:'2026-08-08',supplier:'El Mexicano Foods',products:5,total:14220,status:'IN TRANSIT',expected:'2026-08-09',buyer:'Angel Ramirez'},{id:'PO-22713',date:'2026-08-07',supplier:'La Costeña USA',products:8,total:21350,status:'PARTIAL',expected:'2026-08-08',buyer:'Angel Ramirez'},{id:'PO-22712',date:'2026-08-06',supplier:'Goya Foods',products:3,total:8840,status:'CONFIRMED',expected:'2026-08-12',buyer:'Maria Lopez'},{id:'PO-22711',date:'2026-08-05',supplier:'Alimentos del Pacífico',products:6,total:31700,status:'OPEN',expected:'2026-08-14',buyer:'Angel Ramirez'},{id:'PO-22710',date:'2026-08-04',supplier:'Frutas Selectas MX',products:4,total:17990,status:'OVERDUE',expected:'2026-08-07',buyer:'Maria Lopez'},{id:'PO-22709',date:'2026-08-03',supplier:'La Costeña USA',products:7,total:12640,status:'RECEIVED',expected:'2026-08-06',buyer:'Angel Ramirez'}],salesOrders:[{id:'SO-22698',date:'2026-08-08',customer:'Publico General',products:4,total:7340,status:'READY',due:'2026-08-08',channel:'BULK'},{id:'SO-22697',date:'2026-08-08',customer:'Mercado La Estrella',products:7,total:12480,status:'CONFIRMED',due:'2026-08-09',channel:'DISTRIBUTOR'},{id:'SO-22696',date:'2026-08-07',customer:'Fiesta Foods Houston',products:5,total:9870,status:'PARTIAL',due:'2026-08-08',channel:'BULK'},{id:'SO-22695',date:'2026-08-07',customer:'Amazon FBA',products:3,total:6220,status:'PICKED',due:'2026-08-10',channel:'AMAZON'},{id:'SO-22694',date:'2026-08-06',customer:'La Michoacana Dallas',products:8,total:15890,status:'SHIPPED',due:'2026-08-07',channel:'DISTRIBUTOR'},{id:'SO-22693',date:'2026-08-05',customer:'Super Mercado Rey',products:6,total:11180,status:'HOLD',due:'2026-08-09',channel:'RETAIL'}],products:[{id:'PRD-001',name:'Chile Ancho',category:'Chiles',onHand:5420,committed:640,incoming:1400,cost:2.11,price:2.58,margin:18.2,status:'Active',reorder:4800,weekly:1680},{id:'PRD-002',name:'Jamaica Flor',category:'Spices & Herbs',onHand:4200,committed:780,incoming:2000,cost:1.83,price:2.42,margin:24.4,status:'Active',reorder:2500,weekly:720},{id:'PRD-003',name:'Tamarindo Entero',category:'Fruit',onHand:3240,committed:1120,incoming:0,cost:1.09,price:1.38,margin:21,status:'Active',reorder:3000,weekly:1320},{id:'PRD-004',name:'Frijol Peruano',category:'Beans',onHand:6100,committed:440,incoming:3000,cost:1.18,price:1.39,margin:15.1,status:'Active',reorder:3900,weekly:1040},{id:'PRD-005',name:'Ciruela Pasa',category:'Dried Fruit',onHand:1980,committed:260,incoming:0,cost:2.42,price:2.68,margin:9.7,status:'Active',reorder:2200,weekly:610},{id:'PRD-006',name:'Comino Entero',category:'Spices & Herbs',onHand:2890,committed:330,incoming:800,cost:2.95,price:3.74,margin:21.1,status:'Active',reorder:1800,weekly:430},{id:'PRD-007',name:'Ajo Pelado',category:'Ingredients',onHand:1460,committed:520,incoming:900,cost:1.62,price:1.91,margin:15.2,status:'Active',reorder:1700,weekly:840},{id:'PRD-008',name:'Nuez Mitades',category:'Nuts',onHand:980,committed:110,incoming:600,cost:4.87,price:5.65,margin:13.8,status:'Active',reorder:900,weekly:220}],lots:[{product:'Chile Ancho',lot:'MX-A4832',location:'R12-L1-F',units:'32 bags',lbs:800,received:'2026-07-14',expires:'2027-01-14',status:'Healthy'},{product:'Chile Ancho',lot:'MX-A4920',location:'R15-L2-M',units:'44 bags',lbs:1100,received:'2026-07-26',expires:'2027-02-01',status:'Healthy'},{product:'Jamaica Flor',lot:'JAL-7741',location:'R08-L2-F',units:'36 bags',lbs:900,received:'2026-06-11',expires:'2026-08-27',status:'Expiring'},{product:'Ciruela Pasa',lot:'CP-2218',location:'R22-L1-B',units:'25 cases',lbs:500,received:'2026-05-20',expires:'2026-09-08',status:'Watch'},{product:'Tamarindo Entero',lot:'TM-5510',location:'R13-L1-F',units:'40 bags',lbs:1000,received:'2026-07-30',expires:'2027-01-30',status:'Healthy'},{product:'Frijol Peruano',lot:'FP-6621',location:'R18-L3-M',units:'60 bags',lbs:1500,received:'2026-07-19',expires:'2027-07-19',status:'Healthy'}],parties:[{id:'CUST-52',name:'Publico General',type:'Customer',terms:'Due on receipt',orders:21,balance:0,status:'Active'},{id:'CUST-88',name:'Mercado La Estrella',type:'Customer',terms:'Net 30',orders:18,balance:12840,status:'Active'},{id:'CUST-91',name:'Fiesta Foods Houston',type:'Customer',terms:'Net 21',orders:13,balance:7340,status:'Active'},{id:'VEN-17',name:'La Costeña USA',type:'Vendor',terms:'Net 30',orders:14,balance:21350,status:'Active'},{id:'VEN-23',name:'Goya Foods',type:'Vendor',terms:'Net 30',orders:9,balance:8840,status:'Active'},{id:'VEN-31',name:'El Mexicano Foods',type:'Vendor',terms:'Net 15',orders:11,balance:14220,status:'Active'}]};
function statusTone(status){const s=String(status).toUpperCase();if(['RECEIVED','SHIPPED','READY','ACTIVE','HEALTHY','PICKED'].includes(s))return'green';if(['OVERDUE','HOLD','EXPIRING'].includes(s))return'red';if(['PARTIAL','PARTIALLY_RECEIVED','WATCH','IN TRANSIT'].includes(s))return'amber';if(['CONFIRMED','OPEN'].includes(s))return'blue';return'gray'}function titleCase(s){return String(s||'').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase())}function statusPill(s){return `<span class="status ${statusTone(s)}">${titleCase(s)}</span>`}function metric(label,value,note,trend='',tone='good'){return `<article class="metric-card"><span class="metric-label">${label}</span><strong>${value}</strong><small>${note}</small>${trend?`<em class="metric-trend ${tone}">${trend}</em>`:''}</article>`}function button(label,kind='secondary-btn',attrs=''){return `<button class="${kind}" type="button" ${attrs}>${label}</button>`}function panel(title,subtitle,body,action=''){return `<section class="panel"><header class="panel-head"><div><h3>${title}</h3>${subtitle?`<p>${subtitle}</p>`:''}</div>${action}</header>${body}</section>`}function fmtDate(v){return new Date(v+'T12:00:00').toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})}
function renderNav(){const root=document.getElementById('sidebarNav');root.innerHTML=navSections.map(([label,items])=>`<section class="nav-section"><div class="nav-label">${label}</div>${items.map(([id,icon,name])=>`<button class="nav-item ${state.page===id?'active':''}" data-nav="${id}" type="button"><span class="nav-icon">${icon}</span><span>${name}</span></button>`).join('')}</section>`).join('');root.querySelectorAll('[data-nav]').forEach(b=>b.onclick=()=>navigate(b.dataset.nav))}function navigate(page){state.page=page;state.filters={};location.hash=page;renderNav();renderPage()}function setHeading(breadcrumb,title){document.getElementById('breadcrumb').textContent=breadcrumb;document.getElementById('pageTitle').textContent=title}
function renderPage(){const view=document.getElementById('pageView');const renders={overview:renderOverview,purchase:()=>renderOrders('purchase'),sales:()=>renderOrders('sales'),receiving:renderReceiving,shipping:renderShipping,inventory:renderInventory,packing:renderPacking,replenishment:renderReplenishment,products:renderProducts,parties:renderParties,analytics:renderAnalytics,amazon:renderAmazon,scanner:renderScanner,admin:renderAdmin};view.innerHTML=(renders[state.page]||renderOverview)();bindPageInteractions()}
function attention(dot,title,note,action,target){return `<div class="attention-row"><i class="priority-dot ${dot}"></i><div class="attention-copy"><strong>${title}</strong><span>${note}</span></div><button class="ghost-btn attention-action" data-nav-jump="${target}">${action} →</button></div>`}function activity(icon,title,note,time){return `<div class="activity-row"><div class="avatar">${icon}</div><div class="activity-copy"><strong>${title}</strong><span>${note}</span></div><span class="cell-sub">${time}</span></div>`}
function lineChart(){const vals=[48,52,49,63,68,65,74,81],w=520,h=170,p=18,max=Math.max(...vals),min=Math.min(...vals)-5;const pts=vals.map((v,i)=>[p+i*(w-p*2)/(vals.length-1),h-p-(v-min)/(max-min)*(h-p*2)]);const line=pts.map(p=>p.join(',')).join(' ');const area=`${p},${h-p} ${line} ${w-p},${h-p}`;return `<svg viewBox="0 0 ${w} ${h}" class="mini-chart" role="img" aria-label="Eight week sales trend">${[35,70,105,140].map(y=>`<line x1="${p}" y1="${y}" x2="${w-p}" y2="${y}" class="chart-grid"/>`).join('')}<polygon points="${area}" class="chart-area"/><polyline points="${line}" class="chart-line"/>${pts.map(([x,y])=>`<circle cx="${x}" cy="${y}" r="3" fill="#22654f"/>`).join('')}</svg>`}
function productDemandList(){return `<div class="stock-list panel-pad">${data.products.slice(0,5).map(p=>`<div class="stock-item"><div style="display:flex;justify-content:space-between;gap:10px"><strong>${p.name}</strong><strong>${fmtNum.format(p.weekly)} LB/wk</strong></div><span>${fmtNum.format((p.onHand-p.committed)/p.weekly)} weeks free coverage · ${fmtNum.format(p.onHand-p.committed)} LB free</span><div class="progress" style="margin-top:7px"><span style="width:${Math.min(100,(p.onHand/p.reorder)*60)}%"></span></div></div>`).join('')}</div>`}
function renderOverview(){setHeading('Overview','Operations Center');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Saturday, August 8</h2><p>One view of what is moving, what needs attention, and what to do next.</p></div><div class="hero-actions">${button('Receive product','secondary-btn','data-nav-jump="receiving"')}${button('+ New Sales Order','primary-btn','data-nav-jump="sales"')}</div></div><section class="metric-grid">${metric('Inventory value',fmtMoney.format(data.kpis.inventory),'Current estimated lot cost','+2.4%','good')}${metric('Open sales',fmtMoney.format(data.kpis.openSales),'Confirmed and ready orders','8 due today','warn')}${metric('Open purchases',fmtMoney.format(data.kpis.openPurchases),'PO value not fully received','4 in transit','good')}${metric('Warehouse usage',data.kpis.warehouse+'%','294 of 387 active positions','Healthy','good')}</section><section class="grid-2">${panel('Needs Attention','Highest-priority exceptions across the operation',`<div class="attention-list">${attention('red','Chile Ancho is below reorder point','1.8 weeks of free stock','Review','replenishment')}${attention('red','PO-22710 is overdue','Expected Aug 7 · $17,990','Open PO','purchase')}${attention('amber','Jamaica lot JAL-7741 expires soon','900 LB · 19 days remaining','View lot','inventory')}${attention('amber','SO-22698 is due today','4 products · $7,340','Fulfill','shipping')}</div>`)}${panel("Today's Activity",'Warehouse and order movement since start of day',`<div class="activity-list">${activity('⇩','8 receipts','3,420 LB received','10:46 AM')}${activity('▦','21 placements','17 rack · 4 floor','11:18 AM')}${activity('⇧','6 shipments','8,930 LB sent','11:41 AM')}${activity('▣','14 packing moves','2,640 LB posted used','12:07 PM')}</div>`)}</section>${panel('Operational Flow','Current workload from purchasing through fulfillment',`<div class="flow-strip"><div class="flow-step"><span>PURCHASING</span><strong>12</strong><small>open POs · ${fmtMoney.format(94120)}</small></div><div class="flow-step"><span>RECEIVING</span><strong>3</strong><small>due today · 2 active jobs</small></div><div class="flow-step"><span>WAREHOUSE</span><strong>76%</strong><small>occupied · 9 conflicts to review</small></div><div class="flow-step"><span>SHIPPING</span><strong>8</strong><small>due today · 5 ready</small></div></div>`)}<section class="grid-2">${panel('Sales & Inventory Trend','Last 8 weeks · mock operating data',lineChart())}${panel('Fastest-Moving Products','Weekly demand and available coverage',productDemandList())}</section></div>`}
function renderOrders(kind){const purchase=kind==='purchase';setHeading('Orders',purchase?'Purchase Orders':'Sales Orders');const rows=purchase?data.purchaseOrders:data.salesOrders;const statusOptions=[...new Set(rows.map(r=>r.status))];const primary=purchase?'+ New Purchase Order':'+ New Sales Order';return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>${purchase?'Purchasing pipeline':'Customer fulfillment'}</h2><p>${purchase?'Plan incoming inventory and track vendor commitments.':'Track customer orders from confirmation through shipment.'}</p></div><div>${button(primary,'primary-btn',`data-create="${kind}"`)}</div></div><section class="metric-grid">${purchase?metric('Open orders','12','Requires completion')+metric('In transit','4','Expected within 7 days')+metric('Partial receipts','3','Receiving still open')+metric('Open value',fmtMoney.format(94120),'Not fully received'):metric('Open orders','18','Not fully shipped')+metric('Due today','8','5 are ready to send','Needs attention','warn')+metric('Committed stock','18,420 LB','Protected for open orders')+metric('Open value',fmtMoney.format(186430),'Confirmed pipeline')}</section><section class="panel"><div class="toolbar"><div class="field toolbar-search"><label>Search</label><input data-table-search placeholder="${purchase?'PO, supplier, buyer':'SO, customer, channel'}..."></div><div class="field"><label>Status</label><select data-filter-status><option value="">All statuses</option>${statusOptions.map(s=>`<option value="${s}">${titleCase(s)}</option>`).join('')}</select></div><div class="field"><label>Date from</label><input type="date" data-date-from></div><div class="field"><label>Date to</label><input type="date" data-date-to></div>${button('Clear','secondary-btn','data-clear-filters')}</div><div class="filter-chips" data-filter-chips></div>${ordersTable(rows,purchase)}</section></div>`}
function ordersTable(rows,purchase){return `<div class="table-wrap"><table class="data-table"><thead><tr>${purchase?'<th>PO</th><th>Date</th><th>Supplier</th><th>Products</th><th>Total</th><th>Expected</th><th>Status</th><th>Buyer</th>':'<th>SO</th><th>Date</th><th>Customer</th><th>Products</th><th>Total</th><th>Due</th><th>Status</th><th>Channel</th>'}</tr></thead><tbody data-filter-body>${rows.map(r=>`<tr class="clickable" data-search="${Object.values(r).join(' ').toLowerCase()}" data-status="${r.status}" data-date="${r.date}" data-record="${purchase?'po':'so'}:${r.id}"><td><strong>${r.id}</strong></td><td>${fmtDate(r.date)}</td><td><strong>${purchase?r.supplier:r.customer}</strong></td><td>${r.products}</td><td>${fmtMoney.format(r.total)}</td><td>${fmtDate(purchase?r.expected:r.due)}</td><td>${statusPill(r.status)}</td><td>${purchase?r.buyer:r.channel}</td></tr>`).join('')}</tbody></table></div><footer class="table-footer"><span data-result-count>${rows.length} records</span><span>Click any row to inspect or edit</span></footer>`}
function miniScore(label,value){return `<article class="scorecard"><span>${label}</span><strong>${value}</strong></article>`}
function renderReceiving(){setHeading('Warehouse','Receiving');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Receive → plan → place</h2><p>A guided workflow that keeps the PO, product, quantity, and physical placement connected.</p></div></div><section class="scorecards">${miniScore('Due today','3')}${miniScore('Active jobs','2')}${miniScore('Received today','3,420 LB')}${miniScore('Placed','2,860 LB')}${miniScore('Damaged','45 LB')}${miniScore('Open spaces','93')}</section><section class="split-form"><section class="panel form-card"><div class="panel-head" style="padding:0 0 14px;border:0"><div><span class="eyebrow">START RECEIVING</span><h3 style="margin:5px 0 0">New receiving job</h3></div></div><div class="form-grid"><div class="field full"><label>Purchase Order</label><select id="receivePo"><option>PO-22714 · El Mexicano Foods</option><option>PO-22713 · La Costeña USA</option><option>PO-22712 · Goya Foods</option></select></div><div class="field full"><label>Product</label><select><option>Chile Ancho · 120 cases remaining</option><option>Jamaica Flor · 80 bags remaining</option></select></div><div class="field"><label>Quantity received</label><input id="receiveQty" type="number" value="120"></div><div class="field"><label>Damaged / rejected</label><input id="receiveDamaged" type="number" value="0"></div><div class="field"><label>Cases per space</label><input id="receivePerSpace" type="number" value="40"></div><div class="field"><label>Supplier lot</label><input value="MX-A5018"></div></div><div id="receivingPlan" class="notice-banner" style="margin-top:12px"><strong>120 cases accepted</strong> · 3 spaces required · 40 cases per full space</div><div style="margin-top:13px">${button('Start placement →','primary-btn','data-demo-placement')}</div></section><section class="panel form-card"><div class="panel-head" style="padding:0 0 14px;border:0"><div><span class="eyebrow">OPEN JOB</span><h3 style="margin:5px 0 0">PO-22713 · Jamaica Flor</h3></div>${statusPill('PARTIAL')}</div><div class="kv"><span>Receiving progress</span><strong>2 of 4 spaces placed</strong></div><div class="progress"><span style="width:50%"></span></div><div class="kv"><span>Next placement</span><strong>30 bags · 750 LB</strong></div><div class="stock-list"><div class="stock-item"><strong>R08-L2-F</strong><span>30 bags placed · 10:24 AM</span></div><div class="stock-item"><strong>R08-L2-M</strong><span>30 bags placed · 10:31 AM</span></div></div><div style="margin-top:12px">${button('Resume placement','secondary-btn','data-demo-placement')}</div></section></section></div>`}
function renderShipping(){setHeading('Warehouse','Shipping');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Fulfill from actual storage</h2><p>Choose the physical spaces that will satisfy each confirmed order. No forced FIFO.</p></div>${button('Open Sales Orders','secondary-btn','data-nav-jump="sales"')}</div><section class="grid-3">${metric('Ready to ship','5','Orders fully allocatable')}${metric('Due today','8','Across all open orders','3 need attention','warn')}${metric('Selected today','8,930 LB','Already sent from storage')}</section>${panel('Order Queue','Click an order to choose storage locations',`<div class="kanban">${shippingColumn('READY',data.salesOrders.filter(x=>['READY','PICKED'].includes(x.status)))}${shippingColumn('CONFIRMED',data.salesOrders.filter(x=>x.status==='CONFIRMED'))}${shippingColumn('PARTIAL',data.salesOrders.filter(x=>x.status==='PARTIAL'))}${shippingColumn('HOLD',data.salesOrders.filter(x=>x.status==='HOLD'))}</div>`)}</div>`}function shippingColumn(name,rows){return `<section class="kanban-col"><header class="kanban-head"><strong>${name}</strong><span>${rows.length}</span></header>${rows.length?rows.map(r=>`<article class="kanban-card clickable" data-record="so:${r.id}"><strong>${r.id} · ${r.customer}</strong><span>${r.products} products · ${fmtMoney.format(r.total)}</span><span>Due ${fmtDate(r.due)}</span><footer>${statusPill(r.status)}<button class="ghost-btn" data-record="so:${r.id}">Open →</button></footer></article>`).join(''):'<div class="empty">No orders</div>'}</section>`}
function renderInventory(){setHeading('Warehouse','Inventory');const tabs=`<div class="tabs">${[['racks','Rack View'],['list','Inventory List'],['lots','Lots']].map(([id,label])=>`<button class="tab-btn ${state.inventoryTab===id?'active':''}" data-inventory-tab="${id}">${label}</button>`).join('')}</div>`;const body=state.inventoryTab==='racks'?rackView():state.inventoryTab==='list'?inventoryList():lotsView();return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Physical and financial inventory</h2><p>Switch views depending on whether you need to know where stock is, how much is free, or which lots need attention.</p></div>${tabs}</div>${body}</div>`}
function rackView(){const racks=['R08','R12','R13','R15','R18','R22'];const occupancy={R08:7,R12:7,R13:8,R15:5,R18:6,R22:4};const products=['Chile Ancho','Jamaica Flor','Tamarindo','Frijol Peruano','Comino Entero','Ciruela Pasa'];return `<section class="rack-layout"><section class="panel rack-board"><div class="rack-top"><div class="rack-title"><span>WAREHOUSE RACK</span><strong>${state.selectedRack}</strong></div><div>${statusPill('Healthy')}</div></div><div class="rack-grid"><div></div><div class="rack-col">FRONT</div><div class="rack-col">MIDDLE</div><div class="rack-col">BACK</div>${['L3','L2','L1'].map((level,li)=>`<div class="axis">${level}</div>${['F','M','B'].map((bin,bi)=>{const idx=li*3+bi,occupied=idx<occupancy[state.selectedRack],prod=products[(idx+racks.indexOf(state.selectedRack))%products.length];return `<button class="rack-space ${occupied?'occupied':''} ${prod==='Jamaica Flor'&&state.selectedRack==='R08'?'warning':''}" data-rack-space="${state.selectedRack}-${level}-${bin}"><span class="space-id">${state.selectedRack}-${level}-${bin}</span>${occupied?`<strong>${prod}</strong><span>${20+idx*4} bags · ${500+idx*100} LB</span>`:'<strong>Available</strong><span>Empty space</span>'}</button>`}).join('')}`).join('')}</div><div class="rack-selector">${racks.map(r=>`<button class="${r===state.selectedRack?'active':''}" data-rack-select="${r}">${r}</button>`).join('')}</div></section><aside class="panel side-summary"><div><span class="eyebrow">RACK SUMMARY</span><h3 style="margin:5px 0 0">${state.selectedRack}</h3></div><div class="summary-stat"><span>Occupied</span><strong>${occupancy[state.selectedRack]} / 9</strong></div><div class="summary-stat"><span>Available spaces</span><strong>${9-occupancy[state.selectedRack]}</strong></div><div class="summary-stat"><span>Products</span><strong>${Math.min(5,occupancy[state.selectedRack])}</strong></div><div class="summary-stat"><span>Estimated inventory</span><strong>${fmtNum.format(occupancy[state.selectedRack]*720)} LB</strong></div><div><h3 style="font-size:11px">Products in rack</h3><div class="stock-list">${products.slice(0,Math.min(4,occupancy[state.selectedRack])).map((p,i)=>`<div class="stock-item"><strong>${p}</strong><span>${24+i*8} units · ${600+i*200} LB</span></div>`).join('')}</div></div></aside></section>`}
function inventoryList(){return `<section class="panel"><div class="toolbar"><div class="field toolbar-search"><label>Search product</label><input data-table-search placeholder="Product or category..."></div><div class="field"><label>Category</label><select data-category-filter><option value="">All categories</option>${[...new Set(data.products.map(p=>p.category))].map(c=>`<option>${c}</option>`).join('')}</select></div>${button('Clear','secondary-btn','data-clear-filters')}</div><div class="table-wrap"><table class="data-table"><thead><tr><th>Product</th><th>On Hand</th><th>Committed</th><th>Free</th><th>Incoming</th><th>Weeks Cover</th><th>Inventory Value</th><th>Status</th></tr></thead><tbody data-filter-body>${data.products.map(p=>`<tr class="clickable" data-record="product:${p.id}" data-search="${p.name.toLowerCase()} ${p.category.toLowerCase()}" data-category="${p.category}"><td><strong>${p.name}</strong><span class="cell-sub">${p.id} · ${p.category}</span></td><td>${fmtNum.format(p.onHand)} LB</td><td>${fmtNum.format(p.committed)} LB</td><td><strong>${fmtNum.format(p.onHand-p.committed)} LB</strong></td><td>${fmtNum.format(p.incoming)} LB</td><td>${fmtNum.format((p.onHand-p.committed)/p.weekly)}</td><td>${fmtMoney.format(p.onHand*p.cost)}</td><td>${statusPill(p.onHand-p.committed<p.reorder?'Watch':'Healthy')}</td></tr>`).join('')}</tbody></table></div></section>`}
function lotsView(){return `<section class="panel"><div class="toolbar"><div class="field toolbar-search"><label>Search</label><input data-table-search placeholder="Product, lot, location..."></div><div class="field"><label>Risk</label><select data-filter-status><option value="">All lots</option><option>Expiring</option><option>Watch</option><option>Healthy</option></select></div>${button('Clear','secondary-btn','data-clear-filters')}</div><div class="table-wrap"><table class="data-table"><thead><tr><th>Product</th><th>Supplier Lot</th><th>Location</th><th>Units</th><th>LB</th><th>Received</th><th>Expiration</th><th>Risk</th></tr></thead><tbody data-filter-body>${data.lots.map(l=>`<tr class="clickable" data-record="lot:${l.lot}" data-search="${Object.values(l).join(' ').toLowerCase()}" data-status="${l.status}"><td><strong>${l.product}</strong></td><td>${l.lot}</td><td>${l.location}</td><td>${l.units}</td><td>${fmtNum.format(l.lbs)}</td><td>${fmtDate(l.received)}</td><td>${fmtDate(l.expires)}</td><td>${statusPill(l.status)}</td></tr>`).join('')}</tbody></table></div></section>`}
function renderPacking(){setHeading('Warehouse','Packing Area');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Working inventory</h2><p>See what is in Packing now, what moved through today, and what still needs to be posted or returned.</p></div>${button('Move product to Packing','primary-btn','data-packing-move')}</div><section class="metric-grid">${metric('Currently in Packing','3,280 LB','4 active lots')}${metric('Moved in today','4,850 LB','Across 7 movements')}${metric('Returned','1,120 LB','Back to rack storage')}${metric('Used','2,640 LB','Posted consumption')}</section><section class="grid-2">${panel('Currently in Packing','Editable working inventory',`<div class="stock-list panel-pad">${['Tamarindo Entero','Jamaica Flor','Chile Ancho','Frijol Peruano'].map((p,i)=>`<div class="stock-item"><div style="display:flex;justify-content:space-between"><strong>${p}</strong><strong>${[800,600,1180,700][i]} LB</strong></div><span>Lot ${['TM-5510','JAL-7741','MX-A4832','FP-6621'][i]}</span><div style="display:flex;gap:6px;margin-top:8px">${button('Return','secondary-btn','data-toast="Return workflow opened"')}${button('Post usage','primary-btn','data-toast="Usage editor opened"')}</div></div>`).join('')}</div>`)}${panel("Today's Movement",'All Packing transfers and usage',`<div class="activity-list">${activity('⇩','Tamarindo Entero moved in','40 bags · R13-L1-F → Packing','9:12 AM')}${activity('↩','Chile Ancho returned','12 bags · Packing → R12-L1-F','10:34 AM')}${activity('✓','Jamaica usage posted','14 bags · 350 LB used','11:18 AM')}${activity('⇩','Frijol Peruano moved in','28 bags · R18-L3-M → Packing','11:46 AM')}</div>`)}</section></div>`}
function renderReplenishment(){setHeading('Planning','Replenishment');const rows=data.products.map(p=>({...p,free:p.onHand-p.committed,weeks:(p.onHand-p.committed)/p.weekly,suggested:Math.max(0,Math.ceil((p.reorder*1.5-(p.onHand-p.committed+p.incoming))/100)*100)})).sort((a,b)=>a.weeks-b.weeks);return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>What should we buy next?</h2><p>Turn inventory position, weekly demand, incoming stock, and reorder thresholds into a focused purchasing queue.</p></div>${button('Create Purchase Order','primary-btn','data-create="purchase"')}</div><section class="metric-grid">${metric('Reorder alerts','3','Products below calculated threshold','','warn')}${metric('Suggested buy','5,400 LB','Across 4 products')}${metric('Incoming','8,700 LB','Open confirmed purchasing')}${metric('At-risk value',fmtMoney.format(3840),'Expiring within 30 days','','warn')}</section><section class="panel"><div class="toolbar"><div class="field toolbar-search"><label>Search</label><input data-table-search placeholder="Product or category..."></div><div class="field"><label>Show</label><select data-reorder-filter><option value="">All products</option><option value="buy">Suggested buys only</option></select></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Product</th><th>Free Stock</th><th>Weekly Demand</th><th>Weeks Cover</th><th>Incoming</th><th>Reorder Point</th><th>Suggested Buy</th><th>Action</th></tr></thead><tbody data-filter-body>${rows.map(p=>`<tr data-search="${p.name.toLowerCase()} ${p.category.toLowerCase()}" data-reorder="${p.suggested>0?'buy':'ok'}"><td><strong>${p.name}</strong><span class="cell-sub">${p.category}</span></td><td>${fmtNum.format(p.free)} LB</td><td>${fmtNum.format(p.weekly)} LB</td><td><strong>${fmtNum.format(p.weeks)}</strong></td><td>${fmtNum.format(p.incoming)} LB</td><td>${fmtNum.format(p.reorder)} LB</td><td><strong>${p.suggested?fmtNum.format(p.suggested)+' LB':'—'}</strong></td><td>${p.suggested?button('Add to PO','secondary-btn',`data-toast="${p.name} added to draft PO"`):'<span class="muted">Healthy</span>'}</td></tr>`).join('')}</tbody></table></div></section></div>`}
function renderProducts(){setHeading('Master Data','Products');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Product master</h2><p>Search the catalog, inspect operating metrics, and edit product settings without losing context.</p></div>${button('+ Add Product','primary-btn','data-create="product"')}</div><section class="metric-grid">${metric('Active products','148','Current operational catalog')}${metric('Inventory','842,760 LB','Across active lots')}${metric('Categories','12','Product grouping')}${metric('Need attention','7','Reorder, margin, or expiry','','warn')}</section><section class="panel"><div class="toolbar"><div class="field toolbar-search"><label>Search</label><input data-table-search placeholder="Product ID or name..."></div><div class="field"><label>Category</label><select data-category-filter><option value="">All categories</option>${[...new Set(data.products.map(p=>p.category))].map(c=>`<option>${c}</option>`).join('')}</select></div>${button('Clear','secondary-btn','data-clear-filters')}</div><div class="table-wrap"><table class="data-table"><thead><tr><th>Product</th><th>Category</th><th>Inventory</th><th>Free</th><th>Cost / LB</th><th>Sell / LB</th><th>Margin</th><th>Status</th></tr></thead><tbody data-filter-body>${data.products.map(p=>`<tr class="clickable" data-record="product:${p.id}" data-search="${p.id.toLowerCase()} ${p.name.toLowerCase()} ${p.category.toLowerCase()}" data-category="${p.category}"><td><strong>${p.name}</strong><span class="cell-sub">${p.id}</span></td><td>${p.category}</td><td>${fmtNum.format(p.onHand)} LB</td><td>${fmtNum.format(p.onHand-p.committed)} LB</td><td>${fmtMoney2.format(p.cost)}</td><td>${fmtMoney2.format(p.price)}</td><td><strong>${p.margin}%</strong></td><td>${statusPill(p.status)}</td></tr>`).join('')}</tbody></table></div><footer class="table-footer"><span data-result-count>${data.products.length} products</span><span>Click a product for inventory, purchasing, sales and edit controls</span></footer></section></div>`}
function renderParties(){setHeading('Master Data','Customers & Vendors');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Business directory</h2><p>Keep customer and vendor terms, activity, and balances accessible from one place.</p></div>${button('+ Add Party','primary-btn','data-create="party"')}</div><section class="grid-3">${metric('Customers','86','Active customer accounts')}${metric('Vendors','34','Approved purchasing sources')}${metric('Open balances',fmtMoney.format(64590),'Mock A/R + A/P visibility')}</section><section class="panel"><div class="toolbar"><div class="field toolbar-search"><label>Search</label><input data-table-search placeholder="Name or ID..."></div><div class="field"><label>Type</label><select data-party-filter><option value="">All parties</option><option>Customer</option><option>Vendor</option></select></div></div><div class="table-wrap"><table class="data-table"><thead><tr><th>ID</th><th>Name</th><th>Type</th><th>Terms</th><th>Orders</th><th>Open Balance</th><th>Status</th></tr></thead><tbody data-filter-body>${data.parties.map(p=>`<tr class="clickable" data-record="party:${p.id}" data-search="${p.id.toLowerCase()} ${p.name.toLowerCase()}" data-party="${p.type}"><td>${p.id}</td><td><strong>${p.name}</strong></td><td>${p.type}</td><td>${p.terms}</td><td>${p.orders}</td><td>${fmtMoney.format(p.balance)}</td><td>${statusPill(p.status)}</td></tr>`).join('')}</tbody></table></div></section></div>`}
function barChart(){const arr=[12.5,18.2,24.4,21,15.1,9.7,21.1,15.2],labels=['Nuez','Ancho','Jamaica','Tamarindo','Peruano','Ciruela','Comino','Ajo'];return `<div class="bar-chart">${arr.map((v,i)=>`<div class="bar-group"><div class="bar" style="height:${v/26*100}%"></div><span>${labels[i]}</span></div>`).join('')}</div>`}function ranking(rows,key,suffix){return `<div class="stock-list panel-pad">${rows.map((p,i)=>`<div class="summary-stat"><span>${i+1}. ${p.name}</span><strong>${fmtNum.format(p[key])}${suffix.startsWith('%')?'':' '}${suffix}</strong></div>`).join('')}</div>`}
function renderAnalytics(){setHeading('Intelligence','Analytics');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Management intelligence</h2><p>Separate operating decisions from daily execution: margins, demand, inventory risk, and purchasing performance.</p></div><div class="tabs"><button class="tab-btn active">Executive</button><button class="tab-btn">Products</button><button class="tab-btn">Sales</button><button class="tab-btn">Purchasing</button><button class="tab-btn">Warehouse</button></div></div><section class="metric-grid">${metric('Revenue · 30d',fmtMoney.format(268420),'Mock shipped sales','+8.2%','good')}${metric('Gross profit',fmtMoney.format(47780),'Estimated matched cost','+5.6%','good')}${metric('Gross margin','17.8%','Portfolio weighted','+1.4 pts','good')}${metric('Inventory value',fmtMoney.format(842760),'Current lot cost','+2.4%','good')}</section><section class="grid-2">${panel('Revenue Trend','Eight-week operating view',lineChart())}${panel('Margin by Product','Selected high-volume products',barChart())}</section><section class="grid-3">${panel('Highest Demand','Weekly pounds sold',ranking(data.products.slice().sort((a,b)=>b.weekly-a.weekly).slice(0,5),'weekly','LB/wk'))}${panel('Best Margins','Estimated gross margin',ranking(data.products.slice().sort((a,b)=>b.margin-a.margin).slice(0,5),'margin','%'))}${panel('Needs Review','Low margin or low coverage',ranking(data.products.filter(p=>p.margin<16||(p.onHand-p.committed)<p.reorder).slice(0,5),'margin','% margin'))}</section></div>`}
function renderAmazon(){setHeading('Tools','Amazon Outbound');return `<div class="page-stack"><div class="hero-row"><div class="hero-copy"><h2>Amazon outbound</h2><p>A cleaner specialist flow for FBA movements without cluttering general sales fulfillment.</p></div></div><section class="grid-2">${panel('Start outbound','Scan or search a product',`<div class="panel-pad"><div class="field"><label>Product / barcode</label><input placeholder="Scan or type product"></div><div class="form-grid" style="margin-top:10px"><div class="field"><label>Mode</label><select><option>Full cases / bags</option><option>Specific LB</option></select></div><div class="field"><label>Shipment reference</label><input placeholder="FBA shipment ID"></div></div><div style="margin-top:12px">${button('Continue','primary-btn','data-toast="Amazon outbound workspace opened"')}</div></div>`)}${panel('Today','Recent Amazon movements',`<div class="activity-list">${activity('A','Chile Ancho','12 cases · 300 LB','9:14 AM')}${activity('A','Jamaica Flor','8 bags · 200 LB','10:28 AM')}</div>`)}</section></div>`}function renderScanner(){setHeading('Tools','Scanner');return `<div class="page-stack"><div class="hero-copy"><h2>Scanner test</h2><p>Validate labels and identify products, lots, rack locations, and order references before using them operationally.</p></div><section class="panel panel-pad" style="max-width:720px"><div class="field"><label>Scan value</label><input id="scanInput" autofocus placeholder="Scan barcode / QR code here"></div><div id="scanResult" class="notice-banner" style="margin-top:12px">Waiting for a scan.</div></section></div>`}function renderAdmin(){setHeading('System','Admin');return `<div class="page-stack"><div class="hero-copy"><h2>System administration</h2><p>Users, permissions, warehouse configuration, and operational preferences.</p></div><section class="grid-3">${panel('Users','6 active users',`<div class="panel-pad">${button('Manage users','secondary-btn','data-toast="User management opened"')}</div>`)}${panel('Warehouse','43 racks · 387 positions',`<div class="panel-pad">${button('Configure locations','secondary-btn','data-toast="Location settings opened"')}</div>`)}${panel('Preferences','Units, thresholds, alerts',`<div class="panel-pad">${button('Edit preferences','secondary-btn','data-toast="Preferences opened"')}</div>`)}</section></div>`}
function bindPageInteractions(){document.querySelectorAll('[data-nav-jump]').forEach(b=>b.onclick=()=>navigate(b.dataset.navJump));document.querySelectorAll('[data-record]').forEach(el=>el.onclick=e=>{e.stopPropagation();openRecord(el.dataset.record)});document.querySelectorAll('[data-create]').forEach(b=>b.onclick=()=>openCreate(b.dataset.create));document.querySelectorAll('[data-toast]').forEach(b=>b.onclick=()=>toast(b.dataset.toast));document.querySelectorAll('[data-inventory-tab]').forEach(b=>b.onclick=()=>{state.inventoryTab=b.dataset.inventoryTab;renderPage()});document.querySelectorAll('[data-rack-select]').forEach(b=>b.onclick=()=>{state.selectedRack=b.dataset.rackSelect;renderPage()});document.querySelectorAll('[data-rack-space]').forEach(b=>b.onclick=()=>openRackSpace(b.dataset.rackSpace));bindFilters();bindReceivingCalculator();document.querySelector('[data-demo-placement]')?.addEventListener('click',()=>openPlacement());document.querySelector('[data-packing-move]')?.addEventListener('click',()=>openSimpleDrawer('MOVE TO PACKING','Move product to Packing',`<div class="edit-grid"><div class="field full"><label>Product</label><select><option>Tamarindo Entero</option><option>Chile Ancho</option><option>Jamaica Flor</option></select></div><div class="field"><label>From location</label><select><option>R13-L1-F</option><option>R12-L1-F</option></select></div><div class="field"><label>Quantity</label><input value="20"></div></div><div class="drawer-actions">${button('Move inventory','primary-btn','data-save-demo')}</div>`));const scan=document.getElementById('scanInput');if(scan)scan.oninput=()=>document.getElementById('scanResult').innerHTML=scan.value?`<strong>Recognized:</strong> ${scan.value} · Mock lookup ready`:'Waiting for a scan.'}
function bindFilters(){const search=document.querySelector('[data-table-search]'),status=document.querySelector('[data-filter-status]'),cat=document.querySelector('[data-category-filter]'),party=document.querySelector('[data-party-filter]'),reorder=document.querySelector('[data-reorder-filter]'),from=document.querySelector('[data-date-from]'),to=document.querySelector('[data-date-to]'),body=document.querySelector('[data-filter-body]');if(!body)return;const rows=[...body.querySelectorAll('tr')],apply=()=>{let count=0,q=(search?.value||'').trim().toLowerCase();rows.forEach(r=>{const ok=(!q||(r.dataset.search||'').includes(q))&&(!status?.value||r.dataset.status===status.value)&&(!cat?.value||r.dataset.category===cat.value)&&(!party?.value||r.dataset.party===party.value)&&(!reorder?.value||r.dataset.reorder===reorder.value)&&(!from?.value||!r.dataset.date||r.dataset.date>=from.value)&&(!to?.value||!r.dataset.date||r.dataset.date<=to.value);r.hidden=!ok;if(ok)count++});const c=document.querySelector('[data-result-count]');if(c)c.textContent=`${count} record${count===1?'':'s'}`;const chips=document.querySelector('[data-filter-chips]');if(chips){const vals=[];if(q)vals.push(`Search: ${q}`);if(status?.value)vals.push(`Status: ${titleCase(status.value)}`);if(from?.value)vals.push(`From: ${from.value}`);if(to?.value)vals.push(`To: ${to.value}`);chips.innerHTML=vals.map(v=>`<span class="chip">${v}</span>`).join('')}};[search,status,cat,party,reorder,from,to].filter(Boolean).forEach(el=>el.addEventListener(el.type==='search'?'input':'change',apply));document.querySelector('[data-clear-filters]')?.addEventListener('click',()=>{[search,status,cat,party,reorder,from,to].filter(Boolean).forEach(el=>el.value='');apply()});apply()}
function bindReceivingCalculator(){const q=document.getElementById('receiveQty'),d=document.getElementById('receiveDamaged'),p=document.getElementById('receivePerSpace'),plan=document.getElementById('receivingPlan');if(!q||!plan)return;const update=()=>{const accepted=Math.max(0,Number(q.value||0)-Number(d.value||0)),per=Number(p.value||0),spaces=per?Math.ceil(accepted/per):0;plan.innerHTML=`<strong>${fmtNum.format(accepted)} cases accepted</strong> · ${spaces} space${spaces===1?'':'s'} required · ${fmtNum.format(per)} cases per full space`};[q,d,p].forEach(x=>x.addEventListener('input',update))}
function openRecord(ref){const [type,id]=ref.split(':');if(type==='po')return openOrderDrawer(data.purchaseOrders.find(x=>x.id===id),true);if(type==='so')return openOrderDrawer(data.salesOrders.find(x=>x.id===id),false);if(type==='product')return openProductDrawer(data.products.find(x=>x.id===id));if(type==='party')return openPartyDrawer(data.parties.find(x=>x.id===id));if(type==='lot')return openLotDrawer(data.lots.find(x=>x.lot===id))}
function openDrawer(eyebrow,title,html){document.getElementById('drawerEyebrow').textContent=eyebrow;document.getElementById('drawerTitle').textContent=title;document.getElementById('drawerBody').innerHTML=html;document.getElementById('detailDrawer').hidden=false;document.body.style.overflow='hidden';document.querySelectorAll('[data-close-drawer]').forEach(b=>b.onclick=closeDrawer);document.querySelectorAll('[data-save-demo]').forEach(b=>b.onclick=()=>{toast('Mock changes saved');closeDrawer()});document.querySelectorAll('[data-toast]').forEach(b=>b.onclick=()=>toast(b.dataset.toast));document.querySelectorAll('[data-nav-jump]').forEach(b=>b.onclick=()=>{closeDrawer();navigate(b.dataset.navJump)})}function closeDrawer(){document.getElementById('detailDrawer').hidden=true;document.body.style.overflow=''}
function openOrderDrawer(o,purchase){if(!o)return;const who=purchase?o.supplier:o.customer;openDrawer(purchase?'PURCHASE ORDER':'SALES ORDER',o.id,`<section class="drawer-section"><div class="drawer-grid"><div class="kv"><span>${purchase?'Supplier':'Customer'}</span><strong>${who}</strong></div><div class="kv"><span>Status</span>${statusPill(o.status)}</div><div class="kv"><span>Order date</span><strong>${fmtDate(o.date)}</strong></div><div class="kv"><span>${purchase?'Expected':'Due'}</span><strong>${fmtDate(purchase?o.expected:o.due)}</strong></div><div class="kv"><span>Products</span><strong>${o.products}</strong></div><div class="kv"><span>Total</span><strong>${fmtMoney.format(o.total)}</strong></div></div></section><section class="drawer-section"><h3>Products</h3>${['Chile Ancho','Jamaica Flor','Tamarindo Entero'].slice(0,Math.min(3,o.products)).map((p,i)=>`<div class="summary-stat"><span>${p}</span><strong>${20+i*12} ${purchase?'cases':'units'}</strong></div>`).join('')}</section><section class="drawer-section"><h3>Operational status</h3><div class="notice-banner">${purchase?'Receiving can begin as soon as the order arrives.':'Warehouse chooses the actual lots and storage spaces during fulfillment.'}</div></section><div class="drawer-actions">${button('Edit order','secondary-btn','data-toast="Edit mode opened"')}${button(purchase?'Receive':'Fulfill order','primary-btn',`data-nav-jump="${purchase?'receiving':'shipping'}"`)}</div>`)}
function openProductDrawer(p){if(!p)return;openDrawer('PRODUCT PROFILE',p.name,`<section class="drawer-section"><div class="drawer-grid"><div class="kv"><span>Available inventory</span><strong>${fmtNum.format(p.onHand-p.committed)} LB</strong></div><div class="kv"><span>Incoming</span><strong>${fmtNum.format(p.incoming)} LB</strong></div><div class="kv"><span>Cost / LB</span><strong>${fmtMoney2.format(p.cost)}</strong></div><div class="kv"><span>Selling / LB</span><strong>${fmtMoney2.format(p.price)}</strong></div><div class="kv"><span>Gross margin</span><strong>${p.margin}%</strong></div><div class="kv"><span>Weeks coverage</span><strong>${fmtNum.format((p.onHand-p.committed)/p.weekly)}</strong></div></div></section><section class="drawer-section"><h3>Inventory position</h3><div class="summary-stat"><span>On hand</span><strong>${fmtNum.format(p.onHand)} LB</strong></div><div class="summary-stat"><span>Committed</span><strong>− ${fmtNum.format(p.committed)} LB</strong></div><div class="summary-stat"><span>Free</span><strong>${fmtNum.format(p.onHand-p.committed)} LB</strong></div><div class="summary-stat"><span>Incoming</span><strong>+ ${fmtNum.format(p.incoming)} LB</strong></div></section><section class="drawer-section"><h3>Edit product</h3><div class="edit-grid"><div class="field full"><label>Product name</label><input value="${p.name}"></div><div class="field"><label>Category</label><input value="${p.category}"></div><div class="field"><label>Status</label><select><option>Active</option><option>Inactive</option></select></div><div class="field"><label>Reorder point</label><input value="${p.reorder}"></div><div class="field"><label>Default selling price / LB</label><input value="${p.price}"></div></div></section><div class="drawer-actions">${button('Save changes','primary-btn','data-save-demo')}${button('View inventory','secondary-btn','data-nav-jump="inventory"')}${button('Open analytics','secondary-btn','data-nav-jump="analytics"')}</div>`)}
function openPartyDrawer(p){if(!p)return;openDrawer(p.type.toUpperCase(),p.name,`<section class="drawer-section"><div class="drawer-grid"><div class="kv"><span>ID</span><strong>${p.id}</strong></div><div class="kv"><span>Status</span>${statusPill(p.status)}</div><div class="kv"><span>Payment terms</span><strong>${p.terms}</strong></div><div class="kv"><span>Orders</span><strong>${p.orders}</strong></div><div class="kv"><span>Open balance</span><strong>${fmtMoney.format(p.balance)}</strong></div></div></section><section class="drawer-section"><h3>Edit account</h3><div class="edit-grid"><div class="field full"><label>Name</label><input value="${p.name}"></div><div class="field"><label>Terms</label><select><option>${p.terms}</option><option>Net 15</option><option>Net 30</option><option>Due on receipt</option></select></div><div class="field"><label>Status</label><select><option>Active</option><option>Inactive</option></select></div></div></section><div class="drawer-actions">${button('Save changes','primary-btn','data-save-demo')}</div>`)}
function openLotDrawer(l){if(!l)return;openDrawer('LOT',l.lot,`<section class="drawer-section"><div class="drawer-grid"><div class="kv"><span>Product</span><strong>${l.product}</strong></div><div class="kv"><span>Risk</span>${statusPill(l.status)}</div><div class="kv"><span>Location</span><strong>${l.location}</strong></div><div class="kv"><span>Quantity</span><strong>${l.units} · ${l.lbs} LB</strong></div><div class="kv"><span>Received</span><strong>${fmtDate(l.received)}</strong></div><div class="kv"><span>Expiration</span><strong>${fmtDate(l.expires)}</strong></div></div></section><div class="drawer-actions">${button('Move inventory','primary-btn','data-toast="Movement editor opened"')}${button('Open rack','secondary-btn','data-nav-jump="inventory"')}</div>`)}
function openRackSpace(id){openDrawer('RACK SPACE',id,`<section class="drawer-section"><div class="kv"><span>Current product</span><strong>Chile Ancho</strong></div><div class="kv"><span>Lot</span><strong>MX-A4832</strong></div><div class="kv"><span>Quantity</span><strong>32 bags · 800 LB</strong></div></section><section class="drawer-section"><h3>Adjust space</h3><div class="edit-grid"><div class="field"><label>Purchase units</label><input value="32"></div><div class="field"><label>Status</label><select><option>Occupied</option><option>Available</option><option>Blocked</option></select></div></div></section><div class="drawer-actions">${button('Save adjustment','primary-btn','data-save-demo')}${button('Move inventory','secondary-btn','data-toast="Movement workflow opened"')}</div>`)}
function openPlacement(){openDrawer('RECEIVING PLACEMENT','Place Chile Ancho',`<section class="drawer-section"><div class="kv"><span>PO</span><strong>PO-22714</strong></div><div class="kv"><span>Next placement</span><strong>40 cases · 1,000 LB</strong></div><div class="progress"><span style="width:33%"></span></div></section><section class="drawer-section"><h3>Select storage space</h3><div class="rack-selector">${['R12-L1-M','R12-L1-B','R15-L3-F','FLOOR-1'].map(x=>`<button data-toast="Selected ${x}">${x}</button>`).join('')}</div></section><div class="drawer-actions">${button('Place 40 cases','primary-btn','data-save-demo')}</div>`)}
function openCreate(type){if(type==='purchase'||type==='sales'){const purchase=type==='purchase';return openDrawer(purchase?'NEW PURCHASE ORDER':'NEW SALES ORDER',purchase?'Create Purchase Order':'Create Sales Order',`<section class="drawer-section"><div class="edit-grid"><div class="field full"><label>${purchase?'Supplier':'Customer'}</label><select><option>${purchase?'El Mexicano Foods':'Mercado La Estrella'}</option><option>${purchase?'La Costeña USA':'Publico General'}</option></select></div><div class="field"><label>Order date</label><input type="date" value="2026-08-08"></div><div class="field"><label>${purchase?'Expected delivery':'Requested delivery'}</label><input type="date" value="2026-08-11"></div></div></section><section class="drawer-section"><h3>Products</h3><div class="line-builder"><div class="line-row"><div class="field"><label>Product</label><select><option>Chile Ancho</option></select></div><div class="field"><label>Qty</label><input value="40"></div><div class="field"><label>Unit</label><select><option>CASE</option><option>BAG</option><option>LB</option></select></div><div class="field"><label>${purchase?'Cost':'Price'}</label><input value="${purchase?'52.75':'64.50'}"></div><button>×</button></div></div>${button('+ Add product','secondary-btn','data-toast="New line added"')}</section><section class="drawer-section"><div class="order-total"><span>Subtotal</span><strong>$2,580.00</strong></div><div class="order-total"><span>Tax</span><strong>$0.00</strong></div><div class="order-total grand"><span>Total</span><strong>$2,580.00</strong></div></section><div class="drawer-actions">${button(purchase?'Create Purchase Order':'Create Sales Order','primary-btn','data-save-demo')}${button('Save draft','secondary-btn','data-toast="Draft saved"')}</div>`)}if(type==='product')return openSimpleDrawer('NEW PRODUCT','Add Product',`<div class="edit-grid"><div class="field full"><label>Product name</label><input placeholder="Product name"></div><div class="field"><label>Category</label><input placeholder="Category"></div><div class="field"><label>Perishability days</label><input type="number" value="365"></div></div><div class="drawer-actions">${button('Create product','primary-btn','data-save-demo')}</div>`);if(type==='party')return openSimpleDrawer('NEW ACCOUNT','Add Customer or Vendor',`<div class="edit-grid"><div class="field full"><label>Name</label><input placeholder="Business name"></div><div class="field"><label>Type</label><select><option>Customer</option><option>Vendor</option></select></div><div class="field"><label>Terms</label><select><option>Net 30</option><option>Net 15</option><option>Due on receipt</option></select></div></div><div class="drawer-actions">${button('Create account','primary-btn','data-save-demo')}</div>`)}function openSimpleDrawer(eyebrow,title,html){openDrawer(eyebrow,title,html)}function toast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.classList.remove('show'),1800)}
function globalSearch(q){q=q.trim().toLowerCase();if(!q)return;const po=data.purchaseOrders.find(x=>Object.values(x).join(' ').toLowerCase().includes(q));if(po){navigate('purchase');setTimeout(()=>openOrderDrawer(po,true),0);return}const so=data.salesOrders.find(x=>Object.values(x).join(' ').toLowerCase().includes(q));if(so){navigate('sales');setTimeout(()=>openOrderDrawer(so,false),0);return}const p=data.products.find(x=>(x.id+' '+x.name+' '+x.category).toLowerCase().includes(q));if(p){navigate('products');setTimeout(()=>openProductDrawer(p),0);return}const lot=data.lots.find(x=>Object.values(x).join(' ').toLowerCase().includes(q));if(lot){state.inventoryTab='lots';navigate('inventory');setTimeout(()=>openLotDrawer(lot),0);return}toast('No mock record matched that search')}
document.getElementById('globalSearch').addEventListener('keydown',e=>{if(e.key==='Enter')globalSearch(e.currentTarget.value)});document.getElementById('densityToggle').onclick=()=>{state.compact=!state.compact;document.body.classList.toggle('compact',state.compact);toast(state.compact?'Compact table density':'Comfortable table density')};document.getElementById('quickCreate').onclick=()=>openDrawer('QUICK CREATE','Create new',`<div class="quick-create-grid"><button class="quick-tile" data-qc="purchase"><strong>Purchase Order</strong><span>Buy inventory from a vendor</span></button><button class="quick-tile" data-qc="sales"><strong>Sales Order</strong><span>Record customer demand</span></button><button class="quick-tile" data-qc="product"><strong>Product</strong><span>Add to product master</span></button><button class="quick-tile" data-qc="party"><strong>Customer / Vendor</strong><span>Add a business account</span></button></div>`);document.getElementById('notifyButton').onclick=()=>openSimpleDrawer('NOTIFICATIONS','Needs Attention',`<div class="attention-list">${attention('red','PO-22710 overdue','Expected Aug 7 · $17,990','Open','purchase')}${attention('amber','Jamaica expiration risk','900 LB · 19 days','View','inventory')}${attention('amber','SO-22698 due today','4 products · $7,340','Fulfill','shipping')}</div>`);document.addEventListener('click',e=>{const qc=e.target.closest('[data-qc]');if(qc){closeDrawer();setTimeout(()=>openCreate(qc.dataset.qc),0)}});
const initial=location.hash.replace('#','');if(navSections.some(([,items])=>items.some(([id])=>id===initial)))state.page=initial;window.addEventListener('hashchange',()=>{const p=location.hash.replace('#','');if(p&&p!==state.page){state.page=p;renderNav();renderPage()}});renderNav();renderPage();