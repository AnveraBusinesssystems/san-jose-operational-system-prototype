// P2 mobile transaction safety placeholder.
// The previous layer parsed quantities from mock DOM text and was removed before live connection.
// Rebuild validation against typed live balance/order data, with the backend as final authority.
